require "spec_helper"

describe LightwaveLink do
  it "has a version number" do
    expect(LightwaveLink::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
